## Команды
pwd - узнать в какой сейчас директории находится терминал
ls - посмотреть содержимое директории: 
cd - переход между директориями: 
1. cd etc/nginx - переход по абсолютному пути
2. cd .. - переход на уровень выше
3. cd - - переход в предыдущую директорию
4. cd ~ - переход в домашнюю директорию 
5. cd project/myapp - переход по относительному пути 
mkdir - создать директорию 
touch - создает файл. 

cp - копирует файл по принципу (источник)(назначение). Другие варианты использования cp: 
1. cp readme.txt backup.txt - копирует содержимое одного файла в другой файл с другим названием (если backup.txt нет, то он создастся)
2. cp readme.txt backup/readme.txt - копирует содержимое одного файла в другой, находящийся в другой директории. 
3. cp -r project/project_backup - рекурсивно скопировать директорию со всем содержимым.
mv - перемещение файл

## Флаги
-a или -all - показывает все файлы, в том числе и скрытые файлы
--help - вызывает справочник для команды (ls --help)
